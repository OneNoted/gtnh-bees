return {
  archive_size = 32, -- may be raised, never lowered
  complete_imprint = "all", -- supported policy values: "none" or "all"
  driver_module = "gtnh_bees.official_driver",
  limits = {
    breeding=128, archive=256, scanning=40, conversion=128,
    conversion_generations=8, imprint=128, mutation_generations=8, transfer=8,
    robot_attempts=3, robot_timeout=8
  },
  -- Keys must be copied exactly from specialConditions observed on this
  -- installation. Unknown strings remain unmet. Valid policy shapes are:
  -- ["EXACT OBSERVED STRING"]={policy="foundation",foundation="namespace:block"}
  -- ["EXACT OBSERVED STRING"]={policy="satisfied"}
  -- ["EXACT OBSERVED STRING"]={policy="unmet"}
  mutation_conditions = {},
  network = {
    foundation_port=24193, -- finite integer from 1 through 65535
    robot_address="PINNED-ROBOT-MODEM-ADDRESS",
    shared_secret="REPLACE-WITH-AT-LEAST-16-RANDOM-BYTES",
    replay_epoch="REPLACE-WITH-A-NEW-RANDOM-128-BIT-OR-LONGER-EPOCH"
  },
  roles = {
    -- Official bee_housing component. Discovery uses listAllSpecies and
    -- getBeeBreedingData; callback names are not user configuration.
    genetics = {address="BEE-HOUSING-COMPONENT-ADDRESS"},
    bee_storage = {
      address="TRANSPOSER-ADDRESS", side=0, reserved_slot=27,
      -- Exact registry names observed in this installation; do not guess.
      caste_items={
        ["EXACT-PRINCESS-ITEM-NAME"]="princess",
        ["EXACT-DRONE-ITEM-NAME"]="drone",
        ["EXACT-QUEEN-ITEM-NAME"]="queen"
      }
    },
    breeder = {
      address="TRANSPOSER-ADDRESS", side=1,
      -- Output slots must not overlap either input slot.
      princess_slot=1, drone_slot=2, output_slots={3,4,5},
      terminal_stable_polls=2
    },
    scanner = {
      address="TRANSPOSER-ADDRESS", side=2,
      component_address="FORESTRY-ANALYZER-COMPONENT-ADDRESS",
      -- Output slots must not overlap the analyzer input slot.
      input_slot=1, output_slots={2}
    },
    recovery = {address="TRANSPOSER-ADDRESS", side=3, output_slots={1,2,3,4}},
    foundation_modem = {address="PINNED-CONTROLLER-MODEM-ADDRESS"},
    foundation_data = {address="TIER-2-OR-BETTER-DATA-CARD-ADDRESS"}
  }
}
